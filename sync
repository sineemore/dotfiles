#!/bin/sh

set -eu

cd "$(dirname "$0")"

files() {
	git ls-files | grep -v \
		-e .gitignore \
		-e sync \
		-e backup
}

if [ $# -eq 0 ]
then
	files
else
	files | rsync --ignore-missing-args --files-from - "$@"
fi
